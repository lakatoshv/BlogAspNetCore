/**
 * Tag model.
 */
export class Tag {
  /**
   * Tag Id.
   * @param Id number
   * Tag title.
   * @param Title string
   */
  constructor (
      public id: number,
      public title: string,
  ) {}
}
